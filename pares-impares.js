let numeros = [1,2,3,5,17,29,4,6];

for (let i = 0; i <= numeros.length; i++) {
    let numero = numeros [i]
    if ( (numero % 2) === 0) {
    console.log(' el numero ' + pares + ' es un numero par');
    }
    else{
    console.log(' el numero ' + numero + ' es un numero impar');
    }
}   

